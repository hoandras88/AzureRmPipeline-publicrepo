# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: AzureResourceManagerTemplateDeployment@3
  inputs:
    deploymentScope: 'Resource Group'
    azureResourceManagerConnection: 'HALAB01'
    subscriptionId: 'a441b835-c32d-4807-a1b6-07dcc401b158'
    action: 'Create Or Update Resource Group'
    resourceGroupName: 'rg-halab-test-001'
    location: 'West EU'
    templateLocation: 'URL of the file'
    csmFileLink: 'https://raw.githubusercontent.com/hoandras88/AzureRmPipeline-repo/main/azuredeploy.json?token=GHSAT0AAAAAABTFYYTA2NQAYULOYAXJEFPIYSKBH2Q'
#    overrideParameters: '-projectName azuredemo1'
    deploymentMode: 'Incremental'
    deploymentName: 'DeployPipelineTemplate.'
